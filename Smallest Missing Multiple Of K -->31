class Solution {
    public int missingMultiple(int[] nums, int k) {
        HashSet<Integer> set = new HashSet<>();
        for(int num : nums){
            set.add(num);
        }
        int i=1;
        while(i>0){
            if(set.contains(i)){

            }else{
                if(i%k==0){
                    return i;
                }
            }
            i++;
        }
        return 0;
    }
}
